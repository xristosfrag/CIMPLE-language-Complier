program average
	declare x,sum,average,count;
	#main#
	{
		input(x);
		sum :=0;
		count:=0;
		while(x > 1)
		{
			sum := sum + x;
			count:= count+1;
			x := x - 1;
			print(x);
			
		};
		average := sum/count;
		print(average);
	}.