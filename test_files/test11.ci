program m 

    declare x,tmp;

    function f1(in x, in y, inout z)
    
        declare w;

        function f2(in w)

            declare l;

        {
            w := w + l;
            print(w);
            return (x);
        }

    {
        w := z+y;
        if(w < z)
            z := f1(in x,in y,inout z);   
        ;

        return (2);
    }

    procedure p1(in x)

        declare l,f2;

    {
        l := x+l;
        print(l);
        f2 := x;
    }

#main#
{
    tmp := 3;
    if(f1(in 1,in 2,inout tmp) = 2)
    {
        print (x);
    };

    tmp := f1(in tmp);
}.