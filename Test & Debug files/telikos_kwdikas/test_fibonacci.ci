program fibonacci

    declare x;

    function fibonacci(in x)
    {
        if(x<2)
        {
            return(1)
        }else
        {
            return (fibonacci(in x-1)+fibonacci(in x-2))
        }
    }

    # m a i n #
    {
        input(x);
        print(fibonacci(in x))
    }.