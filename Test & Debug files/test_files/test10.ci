program MAX

    declare a,b,c,d,e;

    function max(in x, in y)
        if(x>y)
            return (x);
        else
            return(y);
        ;

    e := max(in max(in max(in k, in f), in b), in max(in c, in d));
.