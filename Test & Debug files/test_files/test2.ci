
#afto to programa elegxei th swsth domh tou programmatos#


program pr #asdj# # skfjfkdld#


    
    declare  x,y;
    declare z;

    x:=y;

    function f(in x , in y)
        #return (x + y * 3);#
        #return (-3 + y -z + 5 + 4 * 8 / 3);#
        #return (a (in (xkkk7 + 3)/5, in a));#

        #if([not [a > x]])
            return (x + y * 3);#
        #x:=3;#

        declare d,y,z;
        declare k;

        #procedure k(inout kkkkkk8)


            declare k;

            return (a (in (xkkk7 + 3)/5, in a));#
        
        declare l;
    
    declare o;

     function f(in x ,in y)
        while (x < >0)
            x:=y
    


.